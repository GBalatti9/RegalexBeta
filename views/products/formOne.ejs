<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head.ejs') %>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <%- include('../partials/header.ejs') %>
    <form action="/products/" method="POST" class="form-radio">
        <% if(formularioCompleto.length > 0) {%>
        <% const primerFormulario = formularioCompleto[0] %>
        <h2><%= primerFormulario.pregunta %></h2>
        <% if(primerFormulario.respuestas) {%>
            <% const respuestas = primerFormulario.respuestas %>
            <% respuestas.forEach(opciones => { %>
                <label for="respuesta<%= opciones.opcion %>">
                    <input type="radio" name="respuesta" id="respuesta<%= opciones.opcion %>" value="<%= `${opciones.texto}` %>">
                    <p style="display: inline;"><%= opciones.texto %></p>
                </label>
            <% }) %>
        <% } %>
        <% } else {%>
            <% if(preguntaRespuestas.pregunta) {%>
                <h2><%= preguntaRespuestas.pregunta %></h2>
                <% preguntaRespuestas.respuestas.forEach(opciones => { %>
                    <label for="respuesta<%= opciones.opcion %>">
                        <input type="radio" name="respuesta" id="respuesta<%= opciones.opcion %>" value="<%= `${opciones.texto}` %>">
                        <p style="display: inline;"><%= opciones.texto %></p>
                    </label>
                <% }) %>
                <% } %>
        <% } %>
        <br>
        <input type="submit" value="Siguiente">

        <!--
        <input type="radio" name="formOne" value="Comer">Comer
        <br>
        <input type="radio" name="formOne" value="Cocinar">Cocinar
        <br>
        <input type="radio" name="formOne" value="Tomar">Tomar
        <br>
        <input type="radio" name="formOne" value="Leer">Leer
        <br>
        <input type="radio" name="formOne" value="Musica">Musica
        <br>
        <input type="radio" name="formOne" value="Deportes">Deportes
        <br>
        <input type="radio" name="formOne" value="Jardineria">Jardineria
        <br>
        <input type="radio" name="formOne" value="Arte">Arte
        <br>
        <input type="hidden" id="selectedValue" name="selectedValue"> 
        <input type="submit" value="Siguiente"> -->
    </form>
</body>

</html>
